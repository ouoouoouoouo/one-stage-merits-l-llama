# One-stage MERITS-L (Llama) — training deps.
#
# The audio branch consumes CACHED CARE features, so neither torchaudio nor the
# CARE codebase is needed at training time. Only the text encoder is live.

torch>=2.1
transformers>=4.43        # Llama-3.1 rope scaling support
tokenizers>=0.19
peft>=0.11
accelerate>=0.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.66
tensorboard>=2.14
wandb>=0.16

# Access to meta-llama/Meta-Llama-3.1-8B is gated on the Hub: accept the licence
# and `huggingface-cli login` before the first run.
